·This folder contains all datasets needed to run Metam.

sampeld_schools.txt contains all join paths that were manually investigated to calculate false positives.

Datasets/basetables folder contains all datasets that were used to run a downstream task.

Datasets/Augmentationsearch contains all datasets that were used to search for augmentation candidates.

Please unzip all folders before running code.